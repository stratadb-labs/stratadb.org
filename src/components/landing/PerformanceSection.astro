---

---

<section class="relative py-32 bg-background-subtle overflow-hidden">
  <!-- Background elements -->
  <div class="absolute inset-0">
    <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[800px] h-[800px] rounded-full bg-terracotta/5 blur-[150px]" />
  </div>

  <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Section header -->
    <div class="max-w-3xl mx-auto text-center mb-20">
      <span class="text-terracotta text-sm font-medium uppercase tracking-widest mb-4 block">
        Performance
      </span>
      <h2 class="text-4xl lg:text-5xl font-bold text-text-primary mb-6">
        Speed when you need it
      </h2>
      <p class="text-xl text-text-secondary">
        Built in Rust with zero-copy operations and async I/O. Benchmarked, measured, proven.
      </p>
    </div>

    <!-- Performance metrics -->
    <div class="grid md:grid-cols-3 gap-8 mb-20">
      <!-- Throughput -->
      <div class="relative group">
        <div class="absolute -inset-px bg-gradient-to-b from-terracotta/30 to-transparent rounded-2xl opacity-0 group-hover:opacity-100 transition-opacity" />
        <div class="relative bg-background border border-white/5 rounded-2xl p-8 text-center h-full">
          <div class="text-6xl lg:text-7xl font-bold text-text-primary mb-2">
            <span class="gradient-text">250K+</span>
          </div>
          <div class="text-lg text-text-muted uppercase tracking-wider mb-4">ops/second</div>
          <p class="text-sm text-text-secondary">
            Single-threaded Cache mode. 800K+ with 4 threads.
          </p>
        </div>
      </div>

      <!-- Latency -->
      <div class="relative group">
        <div class="absolute -inset-px bg-gradient-to-b from-strata-event/30 to-transparent rounded-2xl opacity-0 group-hover:opacity-100 transition-opacity" />
        <div class="relative bg-background border border-white/5 rounded-2xl p-8 text-center h-full">
          <div class="text-6xl lg:text-7xl font-bold text-text-primary mb-2">
            <span class="bg-gradient-to-r from-strata-event to-emerald-400 bg-clip-text text-transparent">&lt;10</span>
          </div>
          <div class="text-lg text-text-muted uppercase tracking-wider mb-4">microseconds</div>
          <p class="text-sm text-text-secondary">
            P99 read latency on fast path. Memory-mapped for speed.
          </p>
        </div>
      </div>

      <!-- Branch switch -->
      <div class="relative group">
        <div class="absolute -inset-px bg-gradient-to-b from-strata-kv/30 to-transparent rounded-2xl opacity-0 group-hover:opacity-100 transition-opacity" />
        <div class="relative bg-background border border-white/5 rounded-2xl p-8 text-center h-full">
          <div class="text-6xl lg:text-7xl font-bold text-text-primary mb-2">
            <span class="bg-gradient-to-r from-strata-kv to-blue-400 bg-clip-text text-transparent">&lt;1</span>
          </div>
          <div class="text-lg text-text-muted uppercase tracking-wider mb-4">millisecond</div>
          <p class="text-sm text-text-secondary">
            Branch creation and switching. Zero-copy fork semantics.
          </p>
        </div>
      </div>
    </div>

    <!-- Durability modes comparison -->
    <div class="bg-background border border-white/5 rounded-2xl overflow-hidden">
      <div class="px-6 py-4 border-b border-white/5">
        <h3 class="text-lg font-semibold text-text-primary">Choose your durability</h3>
        <p class="text-sm text-text-muted mt-1">Three modes to match your use case</p>
      </div>

      <div class="overflow-x-auto">
        <table class="w-full">
          <thead>
            <tr class="border-b border-white/5">
              <th class="px-6 py-4 text-left text-sm font-medium text-text-muted">Mode</th>
              <th class="px-6 py-4 text-left text-sm font-medium text-text-muted">Throughput</th>
              <th class="px-6 py-4 text-left text-sm font-medium text-text-muted">Durability</th>
              <th class="px-6 py-4 text-left text-sm font-medium text-text-muted">Best for</th>
            </tr>
          </thead>
          <tbody>
            <tr class="border-b border-white/5 hover:bg-white/[0.02] transition-colors">
              <td class="px-6 py-4">
                <span class="inline-flex items-center gap-2">
                  <span class="w-2 h-2 rounded-full bg-amber-400" />
                  <span class="font-mono text-text-primary">Cache</span>
                </span>
              </td>
              <td class="px-6 py-4 text-text-secondary font-mono">250K+ ops/s</td>
              <td class="px-6 py-4 text-text-muted">None (memory only)</td>
              <td class="px-6 py-4 text-text-secondary">Testing, caches, development</td>
            </tr>
            <tr class="border-b border-white/5 hover:bg-white/[0.02] transition-colors">
              <td class="px-6 py-4">
                <span class="inline-flex items-center gap-2">
                  <span class="w-2 h-2 rounded-full bg-terracotta" />
                  <span class="font-mono text-text-primary">Standard</span>
                </span>
              </td>
              <td class="px-6 py-4 text-text-secondary font-mono">50K+ ops/s</td>
              <td class="px-6 py-4 text-text-muted">Periodic fsync (batched)</td>
              <td class="px-6 py-4 text-text-secondary">Most production workloads</td>
            </tr>
            <tr class="hover:bg-white/[0.02] transition-colors">
              <td class="px-6 py-4">
                <span class="inline-flex items-center gap-2">
                  <span class="w-2 h-2 rounded-full bg-emerald-400" />
                  <span class="font-mono text-text-primary">Always</span>
                </span>
              </td>
              <td class="px-6 py-4 text-text-secondary font-mono">~500 ops/s</td>
              <td class="px-6 py-4 text-text-muted">fsync every commit</td>
              <td class="px-6 py-4 text-text-secondary">Critical data, zero loss</td>
            </tr>
          </tbody>
        </table>
      </div>
    </div>

    <!-- Trust signals -->
    <div class="mt-16 flex flex-wrap items-center justify-center gap-8 text-text-muted">
      <div class="flex items-center gap-2">
        <svg class="w-5 h-5 text-terracotta" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
        </svg>
        <span class="text-sm">Verified crash recovery</span>
      </div>
      <div class="flex items-center gap-2">
        <svg class="w-5 h-5 text-terracotta" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
        </svg>
        <span class="text-sm">Fuzzing tested</span>
      </div>
      <div class="flex items-center gap-2">
        <svg class="w-5 h-5 text-terracotta" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
        </svg>
        <span class="text-sm">100% memory safe (Rust)</span>
      </div>
      <div class="flex items-center gap-2">
        <svg class="w-5 h-5 text-terracotta" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
        </svg>
        <span class="text-sm">Apache 2.0 licensed</span>
      </div>
    </div>
  </div>
</section>
